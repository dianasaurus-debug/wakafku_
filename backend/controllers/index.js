const AuthUserController = require('backend/controllers/wakif-auth.controller');
const UserController = require('user.controller');
const AdminController = require('admin.controller');

module.exports = {
    AuthUserController,
    UserController,
    AdminController
}
